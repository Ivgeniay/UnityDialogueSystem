using DialogueSystem.Nodes;
using DialogueSystem.Ports;

namespace DialogueSystem.Generators
{
    internal static class Ext
    {
        //internal static void GetVariable(this ClassGen classGen, BasePort node) =>
         //   classGen.VariablesGen.GetVariable(node);
        //internal static void GetDeclaratedPropField(this ClassGen classGen, BaseNode node, BasePort port, bool isAutoproperty = true, Visibility visibility = Visibility.@public, Attribute attribute = Attribute.None) =>
        //    classGen.PropFieldGen.GetDeclaratedPropField(node, port, isAutoproperty, visibility, attribute);
        
    }
}
